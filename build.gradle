group 'scala-workspace'
version '1.0-SNAPSHOT'

apply plugin: 'scala'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.scala-lang:scala-library:2.11.8'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.12'
}